# SDK oficial da Anthropic
anthropic>=0.40.0

# Driver PostgreSQL (mesma biblioteca que ja usamos no script de importacao)
psycopg2-binary>=2.9.9

# Carrega o arquivo .env
python-dotenv>=1.0.0

# SDK Python do MCP (Model Context Protocol) - cliente para o servidor postgres
mcp>=1.0.0
